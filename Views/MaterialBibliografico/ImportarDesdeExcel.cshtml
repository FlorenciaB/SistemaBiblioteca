@{
    ViewData["Title"] = "Importar Libros desde Excel";
}

<div class="container mt-5">
    <h2 class="mb-4 text-center">📚 Importar Libros desde Excel</h2>

    <div class="alert alert-info">
        <strong>📝 Instrucciones para completar el Excel:</strong>
        <ul>
            <li>Descargá el archivo modelo para completar los datos correctamente.</li>
            <li>No modifiques los nombres de las columnas (fila 1).</li>
            <li>En la <strong>fila 2</strong> tenés una guía del tipo de dato esperado (texto, número, fecha, etc.).</li>
            <li>Completá las filas siguientes con tus datos reales.</li>
            <li>
                <strong>Ubicación:</strong> puede ser <code>Biblioteca</code> o <code>Aula</code>.
                <ul>
                    <li>Si elegís <code>Aula</code>, completá también el campo <strong>Aulas</strong> con el nombre del aula correspondiente (por ejemplo: <em>San Martín</em>).</li>
                    <li>Si elegís <code>Biblioteca</code>, podés dejar el campo <strong>Aulas</strong> vacío.</li>
                </ul>
            </li>
            <li>Podés eliminar o dejar vacías las filas de ejemplo antes de importar.</li>
        </ul>
    </div>

    <div class="d-flex justify-content-center mb-4">
        <a asp-action="DescargarModeloExcel" class="btn btn-outline-primary me-3">
            📥 Descargar modelo Excel
        </a>
        <a asp-action="Index" class="btn btn-secondary">
            🔙 Volver al Catálogo
        </a>
    </div>

    <hr />

    <!-- ALERTA DE ÉXITO -->
    @if (TempData["Mensaje"] != null)
    {
        <div class="alert alert-success">
            @TempData["Mensaje"]
        </div>
    }

    <!-- ALERTA DE ERRORES -->
    @if (!ViewData.ModelState.IsValid)
    {
        <div class="alert alert-danger mt-3">
            <ul class="mb-0">
                @foreach (var error in ViewData.ModelState.Values.SelectMany(v => v.Errors))
                {
                    <li>@error.ErrorMessage</li>
                }
            </ul>
        </div>
    }

    <form asp-action="ImportarExcel" enctype="multipart/form-data" method="post" class="mt-4" id="formImportar">
        <div class="mb-3">
            <label for="archivoExcel" class="form-label">Seleccioná el archivo Excel (.xlsx)</label>
            <input type="file" name="archivoExcel" id="archivoExcel" class="form-control" required accept=".xlsx" />
        </div>

        <button type="submit" class="btn btn-success">
            🚀 Importar Libros
        </button>
    </form>

    <!-- MODAL SPINNER -->
    <div class="modal fade" id="modalCargando" tabindex="-1" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered">
            <div class="modal-content text-center p-3">
                <div class="modal-body">
                    <div class="spinner-border text-primary" role="status">
                        <span class="visually-hidden">Cargando...</span>
                    </div>
                    <p class="mt-3">Procesando Excel, por favor esperá...</p>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        document.getElementById("formImportar").addEventListener("submit", function () {
            var modal = new bootstrap.Modal(document.getElementById('modalCargando'));
            modal.show();
        });
    </script>
}
